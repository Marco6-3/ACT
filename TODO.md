# TODO — ACT 双臂叠毛巾任务

> 状态更新（2026-08-27）：下一批只采集 100 条机器人示教，但每条同步录制 3 个外部相机和 2 个腕部相机。先训练 3 个单外部视角模型（各 100 条），再将同一批示教展开为 300 个视角条件样本训练 Mix-1；每个训练样本始终只输入 1 个外部相机和 2 个腕部相机。Mix-1 达到预设成功率门槛后，才继续 Random-2 和 All-3。

> 当前任务边界：不再采集或训练“只接近、不闭合”的跟随模型。每条新示范必须直接完成“接近指定材料角 → 闭合 → 抬升 10 cm → 保持 3 s”。第一阶段主指标是稳定抓角成功率；折叠不是当前训练目标。

完整分阶段路线见 [`docs/MULTIVIEW_RESEARCH_PLAN.md`](./docs/MULTIVIEW_RESEARCH_PLAN.md)。

## P0：100 条五路同步示教与视角混合筛查

### Day 1：相机与记录链路

- [ ] 固定并命名 `front / left / right` 三个外部相机
- [ ] 腕部相机在所有实验条件中保持一致
- [ ] 完成内参、外参和时间同步检查并记录版本
- [ ] 同步保存相机帧、关节状态、gripper command、预测动作和执行动作
- [ ] 自动生成唯一 `trial_id`、`dataset_version`、`config_id` 和 `camera_setup`
- [ ] 抽查训练和推理真正进入网络的 RGB tensor，排除 camera key、crop、翻转和 RGB/BGR 错误

### Day 2：一份同步示教数据

- [ ] 用同一批示教同步记录全部外部和腕部视角
- [ ] 每条示范直接完成双角抓取、抬升 10 cm 和保持 3 s，不混入 approach-only 轨迹
- [ ] 将成功示范作为首轮行为克隆训练集；中断、抓错角、滑脱等失败 episode 单独标注，不混入初始 BC 数据
- [ ] 按完整 episode 划分 train/val/test，禁止按帧随机划分
- [ ] 先按 `source_episode_id` 划分 train/val/test，再在各 split 内展开相机视角
- [ ] 从同一批 episode 派生 E1、E2、E3 和 Mix-1，不重复采集三套动作示教
- [ ] 明确 300 条是视角条件样本，不是 300 条独立 demonstration
- [ ] 为配对测试建立固定布局编号并记录毛巾平移、旋转和褶皱

### Day 3–4：训练筛查模型

- [ ] E1：`external_1 + left_wrist + right_wrist`，100 个样本
- [ ] E2：`external_2 + left_wrist + right_wrist`，100 个样本
- [ ] E3：`external_3 + left_wrist + right_wrist`，100 个样本
- [ ] E4 / Mix-1：三组单外部视角样本合并为 300 个样本；每个样本仍只有 1 个外部相机槽位和 2 个腕部相机
- [ ] 使用相同示教 episode、训练预算、checkpoint 选择规则和随机种子集合
- [ ] 固定 optimizer update 数，或增加最佳单视角重复采样控制，排除 Mix-1 三倍样本带来的训练步数混杂
- [ ] Mix-1 中三个外部视角平衡采样，记录每条样本的 `source_episode_id` 和 `external_camera_id`

### Day 5–6：机器人配对筛查

- [ ] 每个主要条件先执行至少 20 次，并随机化方法执行顺序
- [ ] 每种方法使用相同布局和毛巾初始条件
- [ ] 自动定位 gripper open→close 时刻并导出 `t-10 / t / t+10` 帧
- [ ] 自动或半自动记录左右角点和 gripper 位置
- [ ] 若用彩色点/Marker 自动测量，避免覆盖真实夹持区，并分别记录 marked/unmarked 条件
- [ ] 人工复核自动成功标签和失败类型，不让视觉模型标签直接成为最终真值

### Day 7：自动汇总和 Go/No-Go

- [ ] 自动生成逐 trial CSV、角点误差图、抓取成功率图、失败类型图和 `report.md`
- [ ] 结果产物固定为 `summary.csv / corner_error / grasp_success / failure_modes / report.md`，原始视频仅保存本地或受控存储
- [ ] 核心输出：左角闭合误差、右角闭合误差、双角进入捕获区域比例和稳定抓角成功率
- [ ] 同时输出双角进入捕获区域比例、微抬保持率和置信区间
- [ ] Mix-1 不优于最佳单视角及其训练步数控制：暂停视角混合扩展，回到反馈、控制和接触诊断
- [ ] Mix-1 在多个测试外部视角下达到预设成功率门槛：进入 Random-2 和 All-3
- [ ] Mix-1 只在一个视角表现好：先检查视角采样平衡和相机偏置，不声称已学到跨视角表征

> 历史诊断模型（2026-08-26，已退出当前主线）：已使用 40 条只接近角点、不闭合夹爪的示范训练
> `test1/020000`。checkpoint 已通过离线加载和配置一致性检查，冻结配置见
> [`results/baseline_corner_approach_test1_020000.md`](./results/baseline_corner_approach_test1_020000.md)。
> 该模型只保留为历史 pipeline 诊断记录，不再要求完成 20 次执行验收，也不是五相机直接抓角实验的前置门槛。

> 历史 `M=30` 记录（2026-08-26）：已由人工完成定性真机测试，现象为可以接近/跟随角点；
> 这不是带测量的通过结果。部署端现已加入夹爪保持张开硬保护、D435i 录制契约、
> query/预测/发布动作追踪和 MCAP 离线自动测量。这些工具可复用于诊断，但不再安排张开夹爪的跟随验收。

> 多相机实验（2026-08-27）：下一批示范改为五路 RGB 同步采集。本地主机
> `192.168.1.18` 连接两台腕部 RealSense（`332522075913`、`332322073584`）和
> 外部 Orbbec `CL8384201CG`；NUC `192.168.1.101` 连接两台新外部 RealSense
>（`405622076349`、`310222078614`）。一次采集生成全部模型
> 的配对数据，禁止为不同视角分别重采导致示范轨迹不一致。

## P0：五相机方位实验矩阵

固定 canonical topic：

- `external_1`：`/observation/external_camera_1/color/image_raw`（本地 Orbbec）
- `external_2`：`/observation/external_camera_2/color/image_raw`（NUC RealSense）
- `external_3`：`/observation/external_camera_3/color/image_raw`（NUC RealSense）
- `left_wrist`：`/observation/left_hand_realsense/color/image_raw`（本地 RealSense）
- `right_wrist`：`/observation/right_hand_realsense/color/image_raw`（本地 RealSense）

模型与输入矩阵：

| 实验 | 模型输入 | 训练轨迹 | 主要比较 |
|---|---|---|---|
| `E1` | `external_1 + left_wrist + right_wrist` | 同一批五路采集 | 外部方位 1 |
| `E2` | `external_2 + left_wrist + right_wrist` | 与 E1 完全相同 | 外部方位 2 |
| `E3` | `external_3 + left_wrist + right_wrist` | 与 E1 完全相同 | 外部方位 3 |
| `E4 / Mix-1` | 每条样本从 `external_1/2/3` 取一台 + 两个腕部相机 | 100 个 source episodes 展开为 300 个视角样本 | 跨视角混合训练 |
| `E5 / Random-2` | 随机两台外部相机 + 两个腕部相机 | 仅在 E4 达标后启动 | 两视角互补与缺失视角鲁棒性 |
| `E6 / All-3` | 三台外部相机 + 两个腕部相机 | 仅在 E4 达标后启动 | 同时多视角信息上限 |

采集与训练门禁：

- [x] 记录两台主机的相机型号、USB 序列号和网络拓扑
- [ ] 保存五路首帧拼图，复核左右腕部及三个外部方位标签
- [ ] 人工确认 `external_1/2/3` 的实际方位标签（如 top/left/right），之后不再换位
- [x] 两台主机使用相同 `ROS_DOMAIN_ID=1`，CycloneDDS 绑定有线网段
- [x] `scripts/sync_clock --status delta@192.168.1.101` 达到 `GOOD`（|offset| ≤ 100 us）
- [x] 五路 RGB 均稳定出帧约 30 Hz，录制首帧门禁要求 5/5 通过
- [ ] 录制一次短 MCAP 冒烟，确认五路 stream count 增长、`recorder_drops=0`
- [ ] 所有正式示范同时保存五路 RGB；任何一路缺失或中途停流则整条 episode 作废
- [ ] 先按 source episode 冻结 train/validation/test，再分别导出 `E1/E2/E3/E4`，防止同一示教的不同视角跨 split 泄漏
- [ ] 核对 E1/E2/E3 的 episode/frame/action 完全配对；E4 保留相同 `source_episode_id`
- [ ] 固定相同 train/validation/test 划分、训练步数、随机种子集合和 checkpoint 选择规则
- [ ] 分别训练 E1、E2、E3；完成后训练 E4 / Mix-1
- [ ] E4 使用统一外部相机输入 key，并均衡采样 `external_1/2/3`
- [ ] E4 与单视角模型固定相同 optimizer updates，或增加单视角重复采样控制

评估矩阵：

- [ ] 毛巾方向覆盖 `0/90/180/270°`，位置覆盖左/中/右与近/中/远
- [ ] 四个目标角分别测试；首轮固定背景、光照和毛巾实例
- [ ] 每个模型使用完全相同的测试布局和随机化顺序
- [ ] 每个条件至少 20 次；记录正确角点、有效接触、闭合夹持、抬升 10 cm 后保持 3 s
- [ ] 主指标为稳定抬升后的角点抓取成功率，同时报告分方位成功率和 95% 置信区间
- [ ] E4 分别在 `external_1/2/3 + wrists` 下测试，报告平均、最好和最差视角性能
- [ ] E4 与最佳单外部视角、三个单视角平均值及训练步数控制比较
- [ ] E4 达标后再实现 E5/E6；Random-2 需要固定外部槽位和显式 mask

停止标准：五路时间同步、左右腕部映射、外部方位标签或 MCAP 完整性任一未通过时，
不得开始训练。若 E1/E2/E3 数据划分或动作标签不完全一致，或同一 source episode 的不同视角进入不同 split，比较结果无效。

## P0：冻结基线和记录配置

- [x] 记录数据集版本、checkpoint、随机种子和 checkpoint 选择规则
- [x] 记录图像分辨率、crop 和网络输入分辨率
- [x] 记录预测 horizon `K` 和每次 query 后连续执行步数 `M`（checkpoint：`K=100`、`M=100`；当前真机候选：运行时覆盖 `M=30`）
- [ ] 记录 policy query frequency、robot control frequency 和端到端延迟
- [x] 记录 temporal ensemble 开关、权重和实现版本
- [x] 固定一组可复用的初筛布局并编号（顺序：`C,L,R,F,B,CCW,CW`；实际初始位置由顶视检测器记录，不要求人工量距离）

完成条件：任一结果都能追溯到唯一数据集、checkpoint、配置和初始布局。

## P0：检查实际训练与推理输入

- [x] 从 dataloader 导出至少 20 张真正进入网络的训练图像（`test1/020000` 已导出 20 组、60 张）
- [ ] 从真实推理导出接近阶段的连续网络输入图像
- [ ] 检查目标角可见性和 resize 后角点/夹爪的像素尺度
- [x] 检查 crop、左右翻转、RGB/BGR、camera key 和 camera index（训练输入与三路真实 topic 已核对；仍需保存真实连续帧证据）
- [x] 检查训练与推理预处理是否一致
- [x] 实现图像、观测状态、预测动作和发布动作时间戳保存（待下一批真实 episode 产出证据）

停止标准：如发现输入、归一化、关节顺序或时序错误，暂停算法消融，先修复并重新建立基线。

## P0：测量夹爪物理捕获区域

- [ ] 定义目标角坐标系和 gripper center / yaw 的测量方法
- [ ] 使用可重复控制建立基准抓取
- [ ] 分别改变 `e_x / e_y / e_z / e_yaw`
- [ ] 每个偏移条件至少重复 5 次
- [ ] 闭合后微抬 5–10 mm 验证是否真正夹住
- [ ] 记录毛巾厚度、层数、初始褶皱和夹爪设置
- [ ] 得到成功捕获区域 `C` 和保守捕获阈值

判断标准：后续不再使用主观的“角点附近 1–2 cm”。如果捕获区域本身极小或不稳定，优先改善夹爪几何、闭合方向或抓取验证。

## P0：量化已有模型的粗视觉跟随

- [x] 设计离散左右、前后平移和旋转布局（`C,L,R,F,B,CCW,CW`）
- [ ] 对每个布局记录真实角点位移
- [ ] 记录左右 gripper 最近接位置和闭合位置
- [ ] 估计 `G_x / G_y / G_yaw`
- [ ] 记录左右臂偏置、方差和不对称性
- [ ] 使用相同初始布局进行配对试验

自动测量实现状态：

- [x] 从 `/joint_states` + 双 Piper URDF 计算 `world` 下左右 `gripper_tcp` 轨迹和位移（mm）
- [x] 从固定 D435i 检测蓝毛巾中心、方向和四角（pixel），保存首末叠图和时间序列
- [x] FK 与在线 TF 只读交叉验证：左右 TCP 数值误差均为 `0.0 mm`
- [x] 用已有 `test1/episode_000022` 完成离线冒烟：左/右 TCP 位移 `534.0/322.5 mm`
- [ ] 标定固定 D435i 到 `world` 的外参或桌面 homography；完成前不得把 pixel 与 mm 相减，也不得把探索性 pixel→TCP 回归称为真实 `G`

判断规则：

- [ ] `G > 0` 只支持“视觉有影响”，不等于精确角点跟踪
- [ ] `G ≈ 0`：检查视觉输入、数据变化范围和平均轨迹捷径
- [ ] `0 < G << 1`：优先研究空间表征和监督覆盖
- [ ] `G ≈ 1` 但有稳定偏置：优先检查标定、控制和动作标签

## P0：Late-Perturbation 接触前纠偏实验

- [ ] 在夹爪距离角点约 50、30、15 mm 时分别施加小幅毛巾位移
- [ ] 设置无扰动配对对照
- [ ] 记录从扰动到动作方向改变的响应延迟
- [ ] 计算 `rho = e_(t+Δ) / e_t`
- [ ] 记录两臂是否同步、单独或完全不响应
- [ ] 记录是否最终进入捕获区域

判断规则：

- [ ] 静态跟随存在但晚期扰动无响应：检查执行步数、查询频率、延迟和 temporal ensemble
- [ ] 及时响应且 `rho < 1`，但仍抓取失败：转向闭合条件和接触验证
- [ ] 响应方向正确但振荡或发散：检查控制增益、观测噪声和动作聚合

## 历史诊断：固定场景 Approach-Only Sanity Check（不再执行）

此实验曾用于验证 pipeline，不作为主要科研结果，也不是当前五相机直接抓角实验的前置门槛。

- [x] 固定毛巾、双臂初始位置和相机（历史设置）
- [x] 只做 corner-approach，不闭合、不折叠（历史任务定义）
- [x] 采集只接近角点的示范并训练候选模型（实际 40 条；`test1/020000`）
- [x] 部署入口加入夹爪保持张开的硬保护（`--hold-grippers-open`）
- [x] `M=30` 完成定性真机测试：可接近/跟随角点
- [ ] 不再补做 20 次验收；若未来作为诊断重启，必须另建实验登记

若失败，检查 normalization、denormalization、joint ordering、时序、控制频率、相机和执行链路；若成功，只能说明 pipeline 基本可用。

## P1：Oracle 因果诊断矩阵

- [ ] A：Raw RGB + ACT
- [ ] B：Marker + ACT
- [ ] C：Oracle corner + ACT
- [ ] D：Oracle corner + 笛卡尔控制器
- [ ] 每组使用配对布局，记录接近误差、进入捕获区域比例和双角抓取成功率

解释规则：

- [ ] B、C 显著优于 A：支持空间表征瓶颈
- [ ] C 不优于 A：停止把主要资源投入 corner detector
- [ ] D 显著优于 C：支持策略动作生成或执行问题
- [ ] D 也失败：优先检查标定、控制、夹爪几何和物理容差

## P0：直接抓角点数据采集与验收

这是当前数据采集和训练主任务，不以 approach-only 模型通过为前提。

- [ ] 示范流程：接近两个指定材料角 → 闭合 → 抬升 10 cm → 保持 3 s → 结束
- [ ] 记录左、右和双角夹持成功率
- [ ] 记录闭合时机、夹爪方向和布料层数
- [ ] 区分夹空、夹内侧、单侧抓取和抓后滑脱
- [ ] 主要成功判据：双侧抓对目标材料角、未明显夹入内侧布料、抬升保持期间不滑脱
- [ ] 失败示范单独保留标签，首轮行为克隆只使用通过质量检查的成功示范
- [ ] recovery 和后续折叠作为后续任务，不纳入首轮主要成功率

## P2：反馈带宽与 Action Chunk 消融

不要只改变 `chunk_size`。分别控制：

- [ ] 预测 horizon `K`
- [ ] 每次 query 后执行步数 `M`
- [ ] query frequency
- [ ] temporal ensemble
- [ ] 端到端延迟

最小矩阵：当前基线；相同 `K`、更小 `M`；较小 `K`、相同 `M`；改变 temporal ensemble 的可比条件。

主要指标：late perturbation 响应延迟、`rho`、`E_left/E_right`、双臂进入捕获区域比例、动作平滑性和任务时间。

## P2：视觉表征消融

仅在 Oracle 实验支持空间表征瓶颈后进行。

- [ ] 原始全局 RGB
- [ ] 角点附近局部高分辨率 crop
- [ ] 全局相机 + 斜视相机
- [ ] 全局相机 + wrist camera
- [ ] corner coordinate / heatmap auxiliary head
- [ ] 可视化预测角点和不确定度

## P2：多视角收益机制

仅在 E4 / Mix-1 可靠优于最佳单视角及训练步数控制后进行。

- [ ] 深度：比较小/大相机基线，并报告可获得的 `x/y/z` 角点误差
- [ ] 深度：在所有目标角均无遮挡的 trial 中单独统计收益
- [ ] 遮挡：按相机和目标角记录可见、部分遮挡、完全遮挡
- [ ] 遮挡：推理时逐个 mask 相机并替换错误时间帧
- [ ] 偏差：比较毛巾平移、旋转、褶皱及轻微相机位姿变化
- [ ] 偏差：设置 held-out camera/view，比较单视角专用模型与 Mix-1
- [ ] 正则化：比较 Mix-1、Single-repeat、Random-2 和 All-3
- [ ] 不使用单独的 attention 热图证明相机依赖；必须结合干预后的性能变化

## P3：跨视角材料角点一致性

仅在多视角收益与角点误差/不确定度下降相关后进行。

- [ ] 定义材料角点 `A/B/C/D`，不用图像左上/右上命名
- [ ] 标注每个目标角在各相机中的像素位置和可见性
- [ ] 检查同步与重投影误差显著小于物理捕获区域
- [ ] 建立共享视觉编码器、相机位姿/ID、角点热图头和一致性投影头
- [ ] M0：单视角 ACT
- [ ] M1：普通多视角融合
- [ ] M2：多视角 + 相同角点监督
- [ ] M3：M2 + 跨视角对比一致性
- [ ] M4：M3 + 三维重投影一致性
- [ ] 使用不同材料角点或可靠材料点作为负样本，检查对称角点假负样本
- [ ] 检查表示坍塌、跨视角检索 PCK、二维/三维角点误差
- [ ] 只有 `M3 > M2` 且真实抓取改善，才支持跨视角 loss 的增量贡献

## P4：多视角到单视角压缩

启动条件：All-3 或跨视角一致性模型可靠优于最佳单视角，且 Mix-1 尚未满足部署目标。

- [ ] 训练完整多视角教师作为信息上限
- [ ] 学生只输入部署相机 `front + wrist`
- [ ] 依次消融蒸馏三维角点/不确定度、规范角点表征和 action chunk
- [ ] 与不蒸馏的单视角学生、Mix-1 和 Random-2 公平比较
- [ ] 按部署相机中的角点可见性分层报告结果
- [ ] 教师只在完全遮挡条件有效时，记录单视角不可观测上限，不宣称无损压缩

## P2：候选粗到细系统

- [ ] 全局 ACT 负责长距离粗接近
- [ ] 局部 residual visual servo 预测小幅 `Δx/Δy/Δz/Δyaw`
- [ ] 只有双臂均进入捕获区域且不确定度足够低时才闭合
- [ ] 闭合后微抬验证，失败时回退重抓
- [ ] 与相同数据量和视觉输入的 vanilla ACT 公平比较

## P3：正式研究评估

- [ ] 预注册主要假设、指标和反驳条件
- [ ] 划分训练布局与未见测试布局
- [ ] 加入未见毛巾实例或材质、尺寸、褶皱变化
- [ ] 使用配对初始条件并随机化方法顺序
- [ ] 初步诊断每条件至少 20 次；正式条件根据功效分析确定次数
- [ ] 报告成功率、连续误差分布和置信区间
- [ ] 统计模型包含方法、扰动阶段及其交互，并将布局/毛巾实例作为随机效应
- [ ] 报告每个失败类别、负结果和超参数选择过程

## 当前最近执行顺序

- [x] **1. 冻结当前基线配置，定义 `C,L,R,F,B,CCW,CW` 初筛布局**
- [x] **2. 实现真实推理 query、预测动作、发布动作和时间戳记录**
- [x] **3. 实现 TCP 毫米位移 + 顶视毛巾像素位移自动测量并通过离线门**
- [ ] **4. 完成五路首帧、时钟同步和短 MCAP 完整性门禁**
- [ ] **5. 采集 100 条五路同步直接抓角示范，并先按 source episode 冻结数据 split**
- [ ] **6. 训练并配对测试 E1/E2/E3，建立三个单外部视角的稳定抓角成功率**
- [ ] **7. 训练 E4 / Mix-1（300 个视角条件样本），并与最佳单视角和 Single-repeat 比较**
- [ ] **8. E4 达到预设成功率门槛后，才启动 E5 / Random-2 和 E6 / All-3**
- [ ] **9. 用闭合误差、捕获区域、深度、遮挡和相机干预解释多视角收益**
- [ ] **10. 机制证据支持后，才实现跨视角材料点一致性与单视角蒸馏**
