# TODO — ACT 双臂叠毛巾任务

> 状态更新（2026-08-26）：完整任务成功率接近 0，但毛巾左右平移时机械臂会产生一定同方向响应。老师提出新增同步外部相机。当前先用一周完成 1/2/3-view 现象筛查和自动评估；多视角收益成立后，再做机制归因、跨视角物质点一致性和单视角压缩。

完整分阶段路线见 [`docs/MULTIVIEW_RESEARCH_PLAN.md`](./docs/MULTIVIEW_RESEARCH_PLAN.md)。

## P0：一周多视角现象筛查

### Day 1：相机与记录链路

- [ ] 固定并命名 `front / left / right` 三个外部相机
- [ ] 腕部相机在所有实验条件中保持一致
- [ ] 完成内参、外参和时间同步检查并记录版本
- [ ] 同步保存相机帧、关节状态、gripper command、预测动作和执行动作
- [ ] 自动生成唯一 `trial_id`、`dataset_version`、`config_id` 和 `camera_setup`
- [ ] 抽查训练和推理真正进入网络的 RGB tensor，排除 camera key、crop、翻转和 RGB/BGR 错误

### Day 2：一份同步示教数据

- [ ] 用同一批示教同步记录全部外部和腕部视角
- [ ] 按完整 episode 划分 train/val/test，禁止按帧随机划分
- [ ] 从同一批 episode 派生 V1、V2、V3 条件，不重复采集三套动作示教
- [ ] 明确同步视角不是独立 demonstration，统计时保留原始 episode 身份
- [ ] 为配对测试建立固定布局编号并记录毛巾平移、旋转和褶皱

### Day 3–4：训练筛查模型

- [ ] V1：`front + wrist`
- [ ] V2：`front + left + wrist`
- [ ] V3：`front + left + right + wrist`
- [ ] 使用相同示教 episode、训练预算、checkpoint 选择规则和随机种子集合
- [ ] 增加 V1-repeat 或容量/token 匹配控制，排除模型规模混杂
- [ ] 如算力允许，增加 Random-1：每个 episode 或 action chunk 随机一个外部视角

### Day 5–6：机器人配对筛查

- [ ] 每个主要条件先执行至少 20 次，并随机化方法执行顺序
- [ ] 每种方法使用相同布局和毛巾初始条件
- [ ] 自动定位 gripper open→close 时刻并导出 `t-10 / t / t+10` 帧
- [ ] 自动或半自动记录左右角点和 gripper 位置
- [ ] 若用彩色点/Marker 自动测量，避免覆盖真实夹持区，并分别记录 marked/unmarked 条件
- [ ] 人工复核自动成功标签和失败类型，不让视觉模型标签直接成为最终真值

### Day 7：自动汇总和 Go/No-Go

- [ ] 自动生成逐 trial CSV、角点误差图、抓取成功率图、失败类型图和 `report.md`
- [ ] 结果产物固定为 `summary.csv / corner_error / grasp_success / failure_modes / report.md`，原始视频仅保存本地或受控存储
- [ ] 核心输出：左角闭合误差、右角闭合误差、双角抓取成功率、完整任务成功率
- [ ] 同时输出双角进入捕获区域比例、微抬保持率和置信区间
- [ ] V3 与 V1 接近：暂停多视角方法开发，回到反馈、控制和接触诊断
- [ ] V3 明显优于 V1：进入 P2 机制实验
- [ ] Random-1 已达到目标：优先简单随机视角训练，不直接上蒸馏

## P0：冻结基线和记录配置

- [ ] 记录数据集版本、checkpoint、随机种子和 checkpoint 选择规则
- [ ] 记录图像分辨率、crop 和网络输入分辨率
- [ ] 记录预测 horizon `K` 和每次 query 后连续执行步数 `M`
- [ ] 记录 policy query frequency、robot control frequency 和端到端延迟
- [ ] 记录 temporal ensemble 开关、权重和实现版本
- [ ] 固定一组可复用的配对初始布局并编号

完成条件：任一结果都能追溯到唯一数据集、checkpoint、配置和初始布局。

## P0：检查实际训练与推理输入

- [ ] 从 dataloader 导出至少 20 张真正进入网络的训练图像
- [ ] 从真实推理导出接近阶段的连续网络输入图像
- [ ] 检查目标角可见性和 resize 后角点/夹爪的像素尺度
- [ ] 检查 crop、左右翻转、RGB/BGR、camera key 和 camera index
- [ ] 检查训练与推理预处理是否一致
- [ ] 保存图像、观测状态和动作时间戳

停止标准：如发现输入、归一化、关节顺序或时序错误，暂停算法消融，先修复并重新建立基线。

## P0：测量夹爪物理捕获区域

- [ ] 定义目标角坐标系和 gripper center / yaw 的测量方法
- [ ] 使用可重复控制建立基准抓取
- [ ] 分别改变 `e_x / e_y / e_z / e_yaw`
- [ ] 每个偏移条件至少重复 5 次
- [ ] 闭合后微抬 5–10 mm 验证是否真正夹住
- [ ] 记录毛巾厚度、层数、初始褶皱和夹爪设置
- [ ] 得到成功捕获区域 `C` 和保守捕获阈值

判断标准：后续不再使用主观的“角点附近 1–2 cm”。如果捕获区域本身极小或不稳定，优先改善夹爪几何、闭合方向或抓取验证。

## P0：量化已有模型的粗视觉跟随

- [ ] 设计离散左右、前后平移和旋转布局
- [ ] 对每个布局记录真实角点位移
- [ ] 记录左右 gripper 最近接位置和闭合位置
- [ ] 估计 `G_x / G_y / G_yaw`
- [ ] 记录左右臂偏置、方差和不对称性
- [ ] 使用相同初始布局进行配对试验

判断规则：

- [ ] `G > 0` 只支持“视觉有影响”，不等于精确角点跟踪
- [ ] `G ≈ 0`：检查视觉输入、数据变化范围和平均轨迹捷径
- [ ] `0 < G << 1`：优先研究空间表征和监督覆盖
- [ ] `G ≈ 1` 但有稳定偏置：优先检查标定、控制和动作标签

## P0：Late-Perturbation 接触前纠偏实验

- [ ] 在夹爪距离角点约 50、30、15 mm 时分别施加小幅毛巾位移
- [ ] 设置无扰动配对对照
- [ ] 记录从扰动到动作方向改变的响应延迟
- [ ] 计算 `rho = e_(t+Δ) / e_t`
- [ ] 记录两臂是否同步、单独或完全不响应
- [ ] 记录是否最终进入捕获区域

判断规则：

- [ ] 静态跟随存在但晚期扰动无响应：检查执行步数、查询频率、延迟和 temporal ensemble
- [ ] 及时响应且 `rho < 1`，但仍抓取失败：转向闭合条件和接触验证
- [ ] 响应方向正确但振荡或发散：检查控制增益、观测噪声和动作聚合

## P0：固定场景过拟合 Sanity Check

此实验用于验证 pipeline，不作为主要科研结果。

- [ ] 固定毛巾、双臂初始位置和相机
- [ ] 只做 corner-approach，不闭合、不折叠
- [ ] 采集约 20 条高度一致示范并明显过拟合
- [ ] 至少执行 20 次
- [ ] 使用捕获区域阈值判断是否到达

若失败，检查 normalization、denormalization、joint ordering、时序、控制频率、相机和执行链路；若成功，只能说明 pipeline 基本可用。

## P1：Oracle 因果诊断矩阵

- [ ] A：Raw RGB + ACT
- [ ] B：Marker + ACT
- [ ] C：Oracle corner + ACT
- [ ] D：Oracle corner + 笛卡尔控制器
- [ ] 每组使用配对布局，记录接近误差、进入捕获区域比例和双角抓取成功率

解释规则：

- [ ] B、C 显著优于 A：支持空间表征瓶颈
- [ ] C 不优于 A：停止把主要资源投入 corner detector
- [ ] D 显著优于 C：支持策略动作生成或执行问题
- [ ] D 也失败：优先检查标定、控制、夹爪几何和物理容差

## P1：恢复 Grasp-Only 任务

仅在双臂能够稳定进入捕获区域后进行。

- [ ] 接近两个角 → 满足闭合条件 → 闭合 → 微抬验证 → 结束
- [ ] 记录左、右和双角夹持成功率
- [ ] 记录闭合时机、夹爪方向和布料层数
- [ ] 区分夹空、夹内侧、单侧抓取和抓后滑脱
- [ ] 加入失败回退和重新抓取的最小规则基线

## P2：反馈带宽与 Action Chunk 消融

不要只改变 `chunk_size`。分别控制：

- [ ] 预测 horizon `K`
- [ ] 每次 query 后执行步数 `M`
- [ ] query frequency
- [ ] temporal ensemble
- [ ] 端到端延迟

最小矩阵：当前基线；相同 `K`、更小 `M`；较小 `K`、相同 `M`；改变 temporal ensemble 的可比条件。

主要指标：late perturbation 响应延迟、`rho`、`E_left/E_right`、双臂进入捕获区域比例、动作平滑性和任务时间。

## P2：视觉表征消融

仅在 Oracle 实验支持空间表征瓶颈后进行。

- [ ] 原始全局 RGB
- [ ] 角点附近局部高分辨率 crop
- [ ] 全局相机 + 斜视相机
- [ ] 全局相机 + wrist camera
- [ ] corner coordinate / heatmap auxiliary head
- [ ] 可视化预测角点和不确定度

## P2：多视角收益机制

仅在 V3 配对结果可靠优于 V1 后进行。

- [ ] 深度：比较小/大相机基线，并报告可获得的 `x/y/z` 角点误差
- [ ] 深度：在所有目标角均无遮挡的 trial 中单独统计收益
- [ ] 遮挡：按相机和目标角记录可见、部分遮挡、完全遮挡
- [ ] 遮挡：推理时逐个 mask 相机并替换错误时间帧
- [ ] 偏差：比较毛巾平移、旋转、褶皱及轻微相机位姿变化
- [ ] 偏差：设置 held-out camera/view，比较同时融合与 Random-1
- [ ] 正则化：加入 Random-subset，使用固定相机槽位和显式 mask
- [ ] 不使用单独的 attention 热图证明相机依赖；必须结合干预后的性能变化

## P3：跨视角材料角点一致性

仅在多视角收益与角点误差/不确定度下降相关后进行。

- [ ] 定义材料角点 `A/B/C/D`，不用图像左上/右上命名
- [ ] 标注每个目标角在各相机中的像素位置和可见性
- [ ] 检查同步与重投影误差显著小于物理捕获区域
- [ ] 建立共享视觉编码器、相机位姿/ID、角点热图头和一致性投影头
- [ ] M0：单视角 ACT
- [ ] M1：普通多视角融合
- [ ] M2：多视角 + 相同角点监督
- [ ] M3：M2 + 跨视角对比一致性
- [ ] M4：M3 + 三维重投影一致性
- [ ] 使用不同材料角点或可靠材料点作为负样本，检查对称角点假负样本
- [ ] 检查表示坍塌、跨视角检索 PCK、二维/三维角点误差
- [ ] 只有 `M3 > M2` 且真实抓取改善，才支持跨视角 loss 的增量贡献

## P4：多视角到单视角压缩

启动条件：V3 可靠优于 V1，且 Random-1/Random-subset 尚未满足部署目标。

- [ ] 训练完整多视角教师作为信息上限
- [ ] 学生只输入部署相机 `front + wrist`
- [ ] 依次消融蒸馏三维角点/不确定度、规范角点表征和 action chunk
- [ ] 与不蒸馏的单视角学生、Random-1 和 Random-subset 公平比较
- [ ] 按部署相机中的角点可见性分层报告结果
- [ ] 教师只在完全遮挡条件有效时，记录单视角不可观测上限，不宣称无损压缩

## P2：候选粗到细系统

- [ ] 全局 ACT 负责长距离粗接近
- [ ] 局部 residual visual servo 预测小幅 `Δx/Δy/Δz/Δyaw`
- [ ] 只有双臂均进入捕获区域且不确定度足够低时才闭合
- [ ] 闭合后微抬验证，失败时回退重抓
- [ ] 与相同数据量和视觉输入的 vanilla ACT 公平比较

## P3：正式研究评估

- [ ] 预注册主要假设、指标和反驳条件
- [ ] 划分训练布局与未见测试布局
- [ ] 加入未见毛巾实例或材质、尺寸、褶皱变化
- [ ] 使用配对初始条件并随机化方法顺序
- [ ] 初步诊断每条件至少 20 次；正式条件根据功效分析确定次数
- [ ] 报告成功率、连续误差分布和置信区间
- [ ] 统计模型包含方法、扰动阶段及其交互，并将布局/毛巾实例作为随机效应
- [ ] 报告每个失败类别、负结果和超参数选择过程

## 当前最近执行顺序

- [ ] **1. 完成三个外部相机同步、标定版本和数据记录链路**
- [ ] **2. 冻结当前基线配置、配对布局和物理捕获区域测量方法**
- [ ] **3. 用同一批同步示教派生并训练 V1/V2/V3**
- [ ] **4. 每组先做至少 20 次配对筛查并自动汇总四个核心指标**
- [ ] **5. 同步核对实际网络输入、预测/执行动作和时间戳**
- [ ] **6. V3 有可靠提升后，才做深度/遮挡/视角偏差机制实验**
- [ ] **7. 机制证据支持后，才实现跨视角材料角点一致性**
- [ ] **8. 多视角教师上限成立且随机视角不足后，才做单视角蒸馏**
