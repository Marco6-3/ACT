# 2026-08-26 ACT 叠毛巾失败诊断

> 状态更新：本文保留首次失败诊断时的推理链。后续观察发现，毛巾左右平移时机械臂会产生一定同方向移动。因此“完全未使用视觉”或“只走固定世界坐标平均轨迹”已不再是唯一主解释。最新问题已更新为：策略虽表现出粗粒度视觉响应，但尚未证明能在接触前持续纠偏并将双臂误差压入夹爪的实际捕获区域。最新执行计划见 [`TODO.md`](./TODO.md)。

## 最新实验现象

对新一轮推理进行了多次测试后，当前完整任务成功率几乎为 0。

比“最后夹不住毛巾角”更严重的现象是：

> **机械臂末端在接近阶段就没有稳定到达蓝色方巾两个目标角点附近。**

因此当前排查重点需要从“最后几毫米的夹取误差”前移到：

1. ACT 是否真正利用视觉识别角点；
2. 模型是否只是复现训练数据中的平均动作轨迹；
3. 训练 / 推理数据管线是否存在问题。

当前阶段暂时不建议直接继续增加完整叠毛巾数据量。

---

## 核心判断

当前现象更接近：

```text
模型知道机械臂应该向毛巾附近运动
                ↓
但没有稳定形成“目标就是两个具体角点”的几何表征
                ↓
机械臂走向一个大致合理的位置
                ↓
尚未到达角点，后续抓取已经必然失败
```

因此，首先需要回答：

> **ACT 到底有没有学会根据图像中的毛巾角位置改变机械臂目标位置？**

---

## P0-1：固定场景过拟合 Sanity Check

这是目前最高优先级实验。

将任务极度简化：

- 毛巾位置完全固定；
- 双臂初始位置固定；
- 相机固定；
- 暂时只要求末端运动到两个角点上方约 1–2 cm；
- 不要求闭合 gripper；
- 不要求后续折叠。

只采集约 20 条高度一致的示范，让 ACT 尝试明显过拟合这一个固定场景。

### 预期解释

如果 20 条固定场景仍无法稳定到达角点：

> 大概率不是“数据量太少”，而应优先检查训练 / 推理 pipeline。

如果能够接近 100% 复现：

> 说明 pipeline 基本可用，再逐渐增加毛巾位置变化，研究视觉是否真正参与决策。

---

## P0-2：毛巾位置扰动测试

固定场景过拟合通过后，再给毛巾加入小范围随机变化，例如：

- 左右平移：约 ±20～30 mm；
- 前后平移：约 ±20～30 mm；
- 小角度旋转：约 ±10°。

关键不是直接统计完整叠毛巾成功率，而是观察：

> **毛巾角移动后，机械臂末端目标是否同步跟着移动。**

例如毛巾整体向左移动 30 mm，如果机械臂仍然去原来的世界坐标位置，则说明模型很可能主要在记忆轨迹，而没有真正利用视觉角点信息。

---

## P0-3：直接检查 ACT 实际输入图像

不要只看录制的视频画面，需要从数据集 / dataloader 中随机导出约 20 张真正进入模型的 RGB 图像。

重点检查：

- 两个目标角是否完整可见；
- resize 后角点是否仍具有足够像素；
- crop 是否裁掉毛巾边缘；
- 图像是否出现左右翻转；
- camera key / camera index 是否正确；
- RGB / BGR 顺序是否异常；
- 训练与推理是否使用完全相同的相机；
- gripper 接近时是否严重遮挡角点。

当前“机械臂根本到不了角点”的现象已经足以让数据管线成为重点怀疑对象。

---

## P0-4：Pipeline 检查项

如果固定场景过拟合失败，优先检查：

- action normalization / denormalization；
- joint ordering；
- observation 与 action 的时间对齐；
- camera feed 是否正确；
- 数据集中的帧率与真实控制频率是否一致；
- inference frequency；
- action chunk 的执行方式；
- temporal ensemble；
- dataloader 是否错误读取了 episode / camera；
- 推理初始机器人状态是否与训练分布一致。

此阶段不要首先把问题归因于 ACT 模型容量。

---

## P1：Marker 诊断实验

如果 pipeline 没有明显问题，可以在两个目标角增加高对比度视觉 Marker，仅用于诊断。

比较：

```text
无 Marker → 机械臂目标偏离角点
有 Marker → 机械臂明显能够追踪角点
```

如果 Marker 带来明显改善，可以较强地支持：

> 当前主要瓶颈是角点视觉表征，而不是机械臂运动学或后续 folding policy。

---

## 当前排查树

```text
ACT 到不了角点
        │
        ▼
固定场景 20 条能否过拟合？
       /            \
     不能            能
      │               │
      ▼               ▼
检查数据与控制      增加毛巾位置扰动
pipeline              │
                      ▼
                末端是否跟随角点？
                    /      \
                  不会      会
                   │         │
                   ▼         ▼
              视觉没有学到   再研究精抓、
              corner        chunk、折叠
```

---

## 当前阶段结论

现阶段应把问题表述从：

> “为什么 ACT 抓不住毛巾角？”

进一步前移为：

> **“为什么 ACT 生成的双臂接近轨迹没有把 gripper 带到毛巾角附近？”**

在这个问题没有定位清楚之前，暂时不建议从约 100 条完整示范直接扩展到数百条。
