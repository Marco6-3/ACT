# ACT 双臂叠毛巾：从粗视觉跟随到接触级精确抓角

本仓库记录双臂机械臂使用 ACT（Action Chunking with Transformers）完成叠毛巾任务时的实验现象、失败诊断、研究问题和实验计划。

当前仓库是研究与实验记录仓库，尚未包含 ACT 训练、推理或机器人控制代码。所有“已确认”“解释”和“待验证”结论均应严格区分。

## 1. 任务与当前状态

任务目标：双臂分别抓住蓝色方巾靠近机器人一侧的两个目标角，然后完成后续折叠。

- 策略：ACT；
- 训练数据：约 100 条人类示范；
- 当前完整任务成功率：多次测试中接近 0；
- 最常见失败：双臂在接触前没有同时精确对准两个目标角；
- 已尝试：改变毛巾平移和角度、调整外部相机视角。

## 2. 证据分层

### 已观察

- 毛巾发生左右平移时，机械臂目标会产生一定的同方向变化；
- 机械臂会运动到毛巾的大致区域；
- 最近一次视频中，双臂在未形成明确有效接触前撤回，没有进入后续折叠。

### 当前解释

上述现象说明“完全忽略视觉、只复现固定世界坐标轨迹”已不是唯一主解释。策略至少表现出粗粒度视觉条件响应。

但这不能证明模型已经学会角点级几何定位，也不能证明它在最后几厘米内持续使用最新图像进行闭环纠偏。

```text
毛巾整体位姿变化
        ↓
机械臂产生粗粒度同向响应
        ↓
接近目标区域
        ↓
误差尚未被证明能收敛到夹爪物理抓取容差内
        ↓
双角抓取失败
```

### 尚未确认

- 机械臂跟随毛巾位移的增益是否接近 1；
- 是否存在固定方向的系统偏置；
- 最后 50、30、15 mm 是否仍会响应新的视觉变化；
- 失败主要来自空间表征、动作执行时序、双臂协同还是接触不可观测；
- 当前 ACT 的误差分布是否超出了夹爪的物理捕获区域。

## 3. 当前核心研究问题

> 为什么动作分块模仿策略能够响应可变形目标的粗粒度位姿变化，却无法在接触前将双臂相对误差压入任务的物理抓取容差？空间表征、反馈带宽、双臂耦合和接触可观测性分别起多大作用？

这个问题比“换相机是否提高成功率”或“更短 chunk 是否更好”更强，因为它要求区分不同失败机制，并且能够被实验反驳。

## 4. 任务分解

```text
全局观察与粗接近
        ↓
局部角点—夹爪相对定位
        ↓
接触前闭环纠偏
        ↓
满足闭合条件
        ↓
闭合与微抬验证
        ↓
失败重抓 / 成功后折叠
```

完整成功率应拆分为：

```text
P(完整成功)
= P(双角进入捕获区域)
× P(双角夹持成功 | 已进入捕获区域)
× P(折叠成功 | 已稳定夹持双角)
```

## 5. 竞争假设

### H1：空间表征瓶颈

Marker、局部高分辨率裁剪或 Oracle corner 应显著降低接近误差。

反驳条件：提供可靠角点位置后，末端误差仍没有明显下降。

### H2：时间反馈瓶颈

策略能完成静态粗定位，但动作执行、查询频率、图像延迟或 temporal ensemble 使最后阶段无法及时纠偏。

反驳条件：晚期扰动时策略能够低延迟响应并持续降低误差，但仍然抓取失败。

### H3：接触与夹持瓶颈

闭合前位置已经足够准确，但闭合时机、夹爪方向、布料厚度、滑移或缺少抓取确认造成失败。

反驳条件：闭合前双臂并未进入实测物理捕获区域。

这些假设不是互斥真理；实验目标是估计各因素的贡献，而不是为预设解释寻找支持。

## 6. 核心测量

### 6.1 物理捕获区域

通过受控偏移抓取实验测量：

```text
C = {(e_x, e_y, e_z, e_yaw) | 抓取能够稳定成功}
```

“到达角点附近 1–2 cm”不是合格判据。后续阈值必须来自真实夹爪、毛巾厚度和闭合方向下的捕获区域。

### 6.2 视觉跟随增益

```text
G_x = Δx_gripper / Δx_corner
G_y = Δy_gripper / Δy_corner
G_yaw = Δyaw_gripper / Δyaw_towel
```

同时记录偏置、方差和左右臂差异。仅观察“会跟着动一点”不足以证明角点跟踪成立。

### 6.3 接触前误差收缩

```text
rho = e_(t+Δ) / e_t
```

- `rho < 1`：误差正在收缩；
- `rho ≈ 1`：没有有效纠偏；
- `rho > 1`：误差正在扩大。

### 6.4 阶段指标

- 左右臂最近接误差；
- 闭合瞬间 `E_left`、`E_right`；
- 双臂同时进入捕获区域的比例；
- late perturbation 响应延迟；
- 左右角分别夹持成功率；
- 双角同时夹持成功率；
- 微抬后的抓取保持率；
- 自动重抓成功率；
- 已稳定抓取条件下的折叠成功率；
- 单次任务耗时。

## 7. Action Chunk 实验边界

不能把 `chunk_size` 直接等同于闭环反馈频率。每次实验必须分别记录：

- 预测 horizon；
- 每次 policy query 后实际连续执行的动作步数；
- policy query frequency；
- robot control frequency；
- temporal ensemble 开关和权重；
- 图像采集、模型推理到动作执行的端到端延迟。

更合理的消融是：

```text
prediction horizon K × executed steps per query M × temporal aggregation
```

否则成功率变化无法归因于训练目标、轨迹平滑性还是反馈带宽。

## 8. 候选系统方向

如果实验支持“粗定位有效、精细收敛不足”，优先考虑：

```text
全局 ACT：到达角点附近
        ↓
局部高分辨率视觉伺服：预测小幅 Δx/Δy/Δz/Δyaw
        ↓
误差与不确定度进入捕获区域后闭合
        ↓
微抬验证，失败则回退重抓
```

算法修改只能在诊断后进入主线。Marker、显式关键点、更短 chunk、双视角或触觉都只是候选干预，不应预先当作论文贡献。

## 9. 研究质量要求

- 保留固定场景 sanity check，但不把过拟合成功当成泛化证据；
- 使用相同初始布局进行配对比较；
- 将训练布局与测试布局分开；
- 正式实验加入未见毛巾实例、材质、尺寸或褶皱；
- 报告连续误差分布、失败类型和置信区间，而不只报告平均成功率；
- 预先写明反驳条件和停止标准；
- 如果只在单一相机、单一毛巾和单一任务上有效，结论必须限制在该设置内；
- 不把简单增加 corner head、缩短 chunk 或更换相机本身表述为算法创新。

## 10. 仓库文件

- [`TODO.md`](./TODO.md)：按依赖关系排序的近期执行清单；
- [`DIAGNOSIS_2026-08-26.md`](./DIAGNOSIS_2026-08-26.md)：初始失败诊断及后续状态更新；
- [`docs/RESEARCH_QUESTION.md`](./docs/RESEARCH_QUESTION.md)：研究假设、反驳条件和贡献边界；
- [`docs/EXPERIMENT_PROTOCOL.md`](./docs/EXPERIMENT_PROTOCOL.md)：实验变量、指标和统计协议；
- [`docs/FAILURE_TAXONOMY.md`](./docs/FAILURE_TAXONOMY.md)：失败分类规则；
- [`results/result_schema.csv`](./results/result_schema.csv)：逐次试验结果字段模板。

## 11. 当前最近一步

1. 测量夹爪对毛巾角的物理捕获区域；
2. 使用现有模型测量视觉跟随增益和 late perturbation 响应；
3. 导出并核对实际网络输入、预测动作、执行动作和时间戳。

完成后再根据证据决定是否重新训练、改变视觉表示或加入局部闭环控制。
