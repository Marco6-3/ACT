# 课程式 ACT 双臂毛巾折叠实验

## 摘要

本实验研究一种面向双臂毛巾折叠的分阶段训练方法。第一阶段使用 100 条蓝色正方形毛巾的直接抓角示教训练 ACT；第二阶段部署该策略，在夹爪偏位、即将夹空等状态由操作者接管，并完成抓取、抬升和折叠；第三阶段从基础策略继续微调。最终模型在 150 次随机摆放测试中完成 35/50 次任务，成功率为 70.0%（Wilson 95% CI 56.2%–80.9%），高于直接抓角基础模型的 13/50 和使用相同 HIL 数据从头训练 20k steps 的 10/50。

结果支持课程式 corrective HIL fine-tuning 在当前系统中的工程有效性。它不构成跨硬件、跨任务或跨分布的普遍结论。

## 1. 任务与系统

### 1.1 任务

机器人需要自主完成以下动作：

```text
接近两个目标角 → 双侧夹取 → 抬升且不滑脱 → 折叠 → 释放并保持折叠状态
```

最终评估中的 `success=1` 表示完整任务成功；`0` 表示失败。测试使用蓝、绿、红、黄四条正方形毛巾和一条白色长方形毛巾，每次由操作者随机摆放。

### 1.2 硬件

- 双 Piper follower 机械臂执行策略动作；
- 双 Piper leader 机械臂用于示教和 HIL 接管；
- 一台 Orbbec 顶部相机提供全局视角，左右腕部相机提供局部视角；
- 两侧 follower 的原生夹爪指更换为更细长的定制夹指，以便接近和夹取毛巾角。

细长夹指是最终系统的硬件设计选择，而不是本轮单独研究的变量。实验没有比较原生与定制夹指，因此不能把成功率提升定量归因于该改动。

### 1.3 软件架构

实验运行在 [physical_ai_runtime](https://github.com/dream25851/physical_ai_runtime) 上。这是一个基于 ROS 2 Jazzy、Pixi 和 RMI 的双臂运行环境，负责：

- 启动双 Piper、Orbbec 和左右腕部相机；
- 将三路 RGB 与 14 维机器人状态送入 ACT；
- 以 14 维左右臂和夹爪关节参考执行动作块；
- 管理 Policy 与 Teleop 的控制权；
- 在 HIL 采集时记录策略前缀、人工接管区间和完整 episode；
- 将 MCAP 轨迹校验并转换为 LeRobot 数据集。

数据流如下：

```text
三路 RGB + joint state
          │
          ▼
 ACT checkpoint ──Policy──┐
                          ▼
                     RMI authority ──► 双 Piper follower
                          ▲
 双 Piper leader ─Teleop──┘
          │
          └──── Episode recorder ─► MCAP ─► LeRobot ─► ACT training
```

## 2. 前期诊断

实验初期，完整折叠几乎不能稳定完成。机械臂通常能够朝毛巾移动，但落点没有稳定收敛到两个指定角。问题因此从“夹爪为什么夹不住”前移为“视觉与控制是否把末端送入真实捕获区域”。

08-26 至 08-29 的诊断得到几条直接影响最终方案的结论：

- “到达毛巾附近”与“正确抓住双角”必须分开评价；
- E1/E2/E3 的初步抓取筛查结果为 3/10、6/10、8/10，说明数据与视角选择会明显改变结果；
- 将 E3 checkpoint 直接切换到两个未参与训练的 D435i 顶视角时均为 0/5，说明相机输入分布必须匹配；
- E3 在五个固定位置、`M=30` 时只有 2/5 正确双角抓取；减小执行步长后虽然更常抓到布，正确角点并未同步提高；
- 同步推理会阻塞控制推进，等待推理时重复发布 hold 也会改变控制输入。最终运行方式改为后台推理，等待期间由控制器保持上一目标；
- 成功示教中的左右闭合时序存在差异，数据一致性仍是影响精细操作的重要因素。

这些筛查没有成为最终主结果，但它们改变了研究方向：继续堆叠相机或均匀增加示教的收益不明确，更值得采集的是当前策略真实会失败的状态。

## 3. 训练路线

### 3.1 直接抓角预训练

基础数据集包含 100 个蓝色正方形毛巾 episode、35,474 帧。模型输入为 Orbbec 顶视图、左右腕部 RGB 与 14 维机器人状态，输出为 14 维双臂与夹爪动作。

| 配置 | 数值 |
|---|---|
| Policy | ACT，ResNet-18 backbone |
| Prediction horizon | `K=150` |
| Training steps | 20k |
| Batch size | 4 |
| Seed | 1000 |
| Validation split | 无 |

该模型记为 `base20k`。现场观察与最终测试都表明，它在中间位置更容易成功，在边缘位置容易失败；由于最终随机摆放没有保存坐标，这一位置效应只保留为定性观察。

### 3.2 Corrective HIL 数据

采集时先由 `base20k` 自主运行。出现夹爪偏位、即将夹空或需要纠偏的状态时，操作者通过双 leader 接管，并完成后续完整任务。

- 101 个 episode、54,791 帧；
- 红、绿正方形毛巾；
- 每条轨迹恰有一次 teleop interval；
- 轨迹同时包含策略访问到的困难状态、人工恢复动作和任务后半段折叠动作。

这种数据与纯成功示教不同：它从策略自身的状态分布出发，把有限的数据预算放在失败边界附近。方法可视为 corrective demonstration fine-tuning 或 DAgger 风格的数据聚合，但没有进行多轮全覆盖标注，因此不是严格的标准 DAgger。

### 3.3 两种 HIL 训练方式

| 模型 | 初始化 | 数据 | HIL 训练步数 |
|---|---|---|---:|
| `scratch20k` | 随机初始化 | HIL-101 | 20k |
| `finetune10k` | `base20k` | HIL-101 | 10k |

二者使用相同 HIL 数据，但初始化和训练步数同时不同。因此最终对照证明的是“预训练 + HIL 微调”整条路线的优势，不能把差异完全解释为单一的初始化效应。

## 4. 最终评估

### 4.1 设计

最终测试包含 3 个模型 × 5 条毛巾 × 10 次，共 150 次。毛巾每次随机摆放，没有固定中心布局，也没有保存位移和朝向。颜色与训练暴露如下：

| 毛巾 | 形状 | 训练暴露 |
|---|---|---|
| 蓝 | 正方形 | 仅直接抓角预训练 |
| 绿 | 正方形 | HIL |
| 红 | 正方形 | HIL |
| 黄 | 正方形 | 未见颜色与实例 |
| 白 | 长方形 | 未见颜色与实例，同时改变形状 |

### 4.2 总体结果

| 模型 | 成功 / 总数 | 成功率 | Wilson 95% CI |
|---|---:|---:|---:|
| `base20k` | 13 / 50 | 26.0% | 15.9%–39.6% |
| `scratch20k` | 10 / 50 | 20.0% | 11.2%–33.0% |
| `finetune10k` | 35 / 50 | 70.0% | 56.2%–80.9% |

微调模型相对 `base20k` 提高 44 个百分点，相对 `scratch20k` 提高 50 个百分点。五个毛巾条件上的成功数均高于两个对照，因此总体提升不是由某一种颜色单独造成的。

### 4.3 分条件结果

| 毛巾 | `base20k` | `scratch20k` | `finetune10k` |
|---|---:|---:|---:|
| 蓝 | 5 / 10 | 0 / 10 | 8 / 10 |
| 绿 | 3 / 10 | 2 / 10 | 9 / 10 |
| 红 | 2 / 10 | 3 / 10 | 8 / 10 |
| 黄 | 1 / 10 | 2 / 10 | 6 / 10 |
| 白 | 2 / 10 | 3 / 10 | 4 / 10 |

`finetune10k` 在红、绿 HIL 内分布上合计为 17/20。黄色未见正方形为 6/10，白色未见长方形为 4/10：模型可以迁移到训练之外的毛巾，但分布变化越大，性能下降越明显。

### 4.4 原始成功编码

每个字符串包含同一条件的 10 次结果，`1` 为成功、`0` 为失败。白色微调的原始记录写作 `101001 0100`，连接后为 `1010010100`。

| 模型 | 蓝 | 绿 | 红 | 黄 | 白 |
|---|---|---|---|---|---|
| `base20k` | `0010110011` | `0000101010` | `0000001001` | `0100000000` | `1000000100` |
| `scratch20k` | `0000000000` | `1000000100` | `1100000001` | `1000000010` | `0011000010` |
| `finetune10k` | `1110011111` | `1110111111` | `1111101101` | `0110011011` | `1010010100` |

机器可读版本见 [results.csv](./results.csv)。

## 5. 结论与限制

实验回答了一个实用问题：在已有直接抓角能力的前提下，让策略先进入真实失败状态，再由人完成恢复和后续任务，能够显著改善完整毛巾折叠表现。HIL 数据从头训练 20k steps 仍只有 20%，而继承微调 10k steps 达到 70%，说明基础技能与纠偏数据的衔接比单独使用其中一类数据更重要。

结论的边界包括：

- 单一硬件、相机配置和训练 seed；
- 每个模型 × 毛巾条件只有 10 次，条件级置信区间较宽；
- 随机摆放的位置和朝向没有记录，无法验证模型间位置难度是否平衡；
- 只有条件级 0/1 编码，没有逐 trial 视频、阶段标签、时间和折叠误差；
- 白色条件同时改变颜色、实例和形状，不能单独归因于形状；
- 细长夹指、HIL 困难状态采样和完整折叠标签都可能影响最终表现，本轮没有做独立消融。

因此，本阶段最合适的表述是：**课程式 corrective HIL fine-tuning 在当前双 Piper 毛巾折叠系统中取得了明确的工程效果，并显示出有限但可观察的跨毛巾迁移能力。**
